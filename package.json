{
  "name": "Paragon",
  "version": "0.0.3",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bump:patch": "npm version patch && git push",
    "bump:minor": "npm version minor && git push",
    "bump:major": "npm version major && git push"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {},
  "engines": {
    "node": ">=0.12"
  },
  "dependencies": {
    "express": "^4.12.0",
    "mongoose": "~3.8.0"
  }
}
